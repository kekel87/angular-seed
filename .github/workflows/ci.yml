name: CI
on: push
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true
jobs:
  install:
    name: 🚧 Install
    runs-on: ubuntu-latest
    container: cypress/browsers:node14.15.0-chrome96-ff94
    steps:
      - uses: actions/checkout@v2
      - uses: ng-easy/npm-setup@v1

  lint:
    name: 👮 Lint
    runs-on: ubuntu-latest
    container: cypress/browsers:node14.15.0-chrome96-ff94
    needs: install
    steps:
      - uses: actions/checkout@v2
      - uses: ng-easy/npm-setup@v1
      - run: npm run lint

  test:
    name: ✔ Unit tests
    runs-on: ubuntu-latest
    container: cypress/browsers:node14.15.0-chrome96-ff94
    needs: install
    steps:
      - uses: actions/checkout@v2
      - uses: ng-easy/npm-setup@v1
      - run: npm run test

  e2e:
    name: 🐵 E2E tests
    runs-on: ubuntu-latest
    container: cypress/browsers:node14.15.0-chrome96-ff94
    needs: install
    steps:
      - uses: actions/checkout@v2
      - uses: ng-easy/npm-setup@v1
      - run: npm run e2e -- -c headless

  build:
    name: 🛠️ Build
    runs-on: ubuntu-latest
    container: cypress/browsers:node14.15.0-chrome96-ff94
    needs: install
    steps:
      - uses: actions/checkout@v2
      - uses: ng-easy/npm-setup@v1
      - run: npm run build  -- --prod --base-href=https://kekel87.github.io/angular-seed/
      - uses: actions/upload-artifact@v2
        with:
          name: dist
          path: dist/seed
          retention-days: 1

  deploy:
    name: 🚀 Deploy on pages
    runs-on: ubuntu-latest
    needs: [lint, test, e2e, build]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/download-artifact@v2
        with:
          name: dist
          path: dist
      - uses: JamesIves/github-pages-deploy-action@v4.2.2
        with:
          branch: gh-pages
          folder: dist
